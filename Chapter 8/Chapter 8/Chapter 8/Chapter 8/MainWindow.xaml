<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Chapter_8.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Chapter_8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Chapter 8">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" RowDefinitions="Auto,Auto,*" Padding="20">

        <!-- Resources -->
        <Grid.Resources>
            <Storyboard x:Key="RectangleAnimation">
                <DoubleAnimation
                Storyboard.TargetName="AnimatedRectangle"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                From="1"
                To="3"
                Duration="0:0:2" />
            </Storyboard>
        </Grid.Resources>

        <!-- Greeting Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Enter Your Name:" HorizontalAlignment="Center" />
            <TextBox x:Name="NameTextBox" Width="200" Margin="5" />
            <Button Content="Greet Me" Click="OnGreetButtonClick" HorizontalAlignment="Center" />
            <TextBlock x:Name="GreetingTextBlock" FontSize="16" Margin="5" HorizontalAlignment="Center"/>
            <TextBlock Text="Data Binding:" HorizontalAlignment="Center" />
            <TextBox Text="{Binding Name}" Width="200" Margin="5" />
        </StackPanel>

        <!-- Animation Section -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button Content="Animate" Click="OnAnimateButtonClick" HorizontalAlignment="Center"/>
            <Rectangle x:Name="AnimatedRectangle"
                   Width="100"
                   Height="100"
                   Fill="Blue"
                   RenderTransformOrigin="0.5,0.5">
                <Rectangle.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </StackPanel>

        <!-- Image Picker Section -->
        <StackPanel Grid.Row="2" x:Name="MainStackPanel" HorizontalAlignment="Center" VerticalAlignment="Top" Spacing="10">
            <Button Content="Open Image" 
        Click="OnOpenFileButtonClick"
        HorizontalAlignment="Center" />

            <Image x:Name="SelectedImage"
        Width="300"
        Height="300"
        Stretch="Uniform"
        HorizontalAlignment="Center"
        VerticalAlignment="Top" />
        </StackPanel>
    </Grid>

</Window>
